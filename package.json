{
  "name": "dsh-plugin-desktop",
  "version": "0.4.2",
  "description": "Companion plugin for DSH-desktop: current-session / usage / prompt / approval APIs, turn stats (TTFT/speed/cache), error surfacing, in-page context menu + pinned sessions, right-side file panel, and the /api/shell PTY terminal bridge.",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client-menu.js",
    "./package.json": "./package.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web",
      "inject": [
        "@deepseek-ai/dsh-client-runtime"
      ]
    }
  },
  "license": "MIT",
  "author": "Quan-Robin",
  "keywords": [
    "dsh",
    "dsh-plugin",
    "desktop",
    "context-menu"
  ],
  "dependencies": {
    "ws": "^8.21.0"
  },
  "scripts": {
    "test": "node test/run.mjs && node test/compat.test.mjs"
  }
}
